import { MascaraCepDirective } from './mascara-cep.directive';

describe('MascaraCepDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraCepDirective();
    expect(directive).toBeTruthy();
  });
});
